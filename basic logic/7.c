#include<stdio.h>
void main()
{
			int w,l,area;
			printf("Enter the width:");
			scanf("%d",&w);
			printf("enter the length:");
			scanf("%d",&l);
			area=w*l;
			printf("Area of rectangle:%d",area);
}
